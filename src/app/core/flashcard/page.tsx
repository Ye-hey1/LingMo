'use client'

import { FlashcardWorkspace } from '@/app/core/main/flashcard/flashcard-workspace'

export default function FlashcardHomePage() {
  return (
    <div className="flex min-h-screen bg-background">
      <FlashcardWorkspace />
    </div>
  )
}
